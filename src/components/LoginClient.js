"use client";import {useState} from "react";import Link from "next/link";import {useRouter} from "next/navigation";import {LockKey} from "@phosphor-icons/react";export default function LoginClient(){const router=useRouter();const [password,setPassword]=useState(""),[error,setError]=useState(""),[busy,setBusy]=useState(false);const submit=async e=>{e.preventDefault();setBusy(true);setError("");const r=await fetch("/api/auth/login",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({password})});if(r.ok)router.push("/admin");else setError((await r.json()).error);setBusy(false)};return <main className="login-page"><form onSubmit={submit}><div className="login-mark">✦</div><h1>CHAYKAHANA</h1><p>Admin panelga kirish</p><label><LockKey/><input type="password" value={password} onChange={e=>setPassword(e.target.value)} placeholder="Admin paroli" required/></label>{error&&<span>{error}</span>}<button disabled={busy}>{busy?"Tekshirilmoqda…":"Kirish"}</button><Link href="/">← Menyuga qaytish</Link></form></main>}
